import React from 'react';
import PageMeta from '../components/PageMeta';

const Workflow: React.FC = () => {
  return (
    <div>
      <PageMeta
        title="From URL to Close | GreendoorAI Workflow"
        description="See the entire GreendoorAI flow, from a single URL to a closed deal."
      />
      <div className="text-center py-24">
        <h1 className="text-4xl font-bold">Workflow</h1>
        <p className="mt-4 text-lg">This is a placeholder for the Workflow page.</p>
      </div>
    </div>
  );
};

export default Workflow; 